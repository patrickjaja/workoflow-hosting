# Workoflow MCP Server Configuration (Development)

# Platform API URL (local development)
WORKOFLOW_API_URL=http://localhost:8000

# Tool filtering (optional, comma-separated)
# Leave empty to expose ALL tools
TOOL_TYPES=

# Cache TTL for tool definitions (seconds)
CACHE_TTL_SECONDS=60
